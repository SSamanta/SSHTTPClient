Pod::Spec.new do |s|
  s.name         = "SSHTTPClient"
  s.version      = "v1.0.0"
  s.source       = { :git => "https://github.com/SSamanta/SSHTTPClient.git", :branch => "master", :tag => "v1.0.0"  }
end
